/**
 * Site-wide configurable links.
 * Update these values — no component code changes needed.
 */
export const siteConfig = {
  /** Destination for the "Meeting" button in the navbar (Cal.com / Calendly / any URL). */
  meetingUrl: "https://cal.com/amangairola/15min",
  meetingLabel: "Meeting",
};